<h2>My Favorite Orgs</h2>

<ul>
  {{#each model as |org|}}
  <li>
    <span {{action "handleClickedFavorite" org}}>[ Favorite ]</span>
    {{link-to org.displayName "org" org.id}}
  </li>
  {{/each}}
</ul>

{{outlet}}
